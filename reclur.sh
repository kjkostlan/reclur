#!/bin/bash
# Set up double click on mac: Open with, other, change recommended apps to all apps, select terminal and select always.
# For windows use the .bat file instead.
cd "$(dirname "$0")"
lein run